## Documentación de contenido no dado en clase pero necesario para el desarrllo del software

1. Comentarios con Doxygen
En clase se trabajó `@param`.
A este se le suman:
    `@class` para indicar qué hace una clase.
    `@return` para indicar qué devuelve un método. Si no devuelve nada se pone `@return void`.
